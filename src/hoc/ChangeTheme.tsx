import React from 'react'
import PropTypes from 'prop-types'

function ChangeTheme(props) {
  return (
    <div>
<p>Change Theme Provider</p>
    </div>
  )
}

.propTypes = {}

export default ChangeTheme

